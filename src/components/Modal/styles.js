import {StyleSheet} from 'react-native';

export default styles = StyleSheet.create({
  container: {
    backgroundColor: 'white',
    position: 'absolute',
    alignItems: 'center',
    zIndex: 99,
  },
  circle: {
    alignSelf: 'center',
    backgroundColor: 'white',
    width: 0,
    height: 0,
    borderRadius: 999,
  },
});
